@page
@model FoodWeb.Pages.Admin.ShowFactsModel
@{
}
<div class="row justify-content-center">
    <div class="justify-content-center col-md-8">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Clients</th>
                    <th scope="col">Hour of Support</th>
                    <th scope="col">Projects</th>
                    <th scope="col">Workers</th>
                    <th scope="col" colspan="2">Action</th>
                </tr>
            </thead>
            <tbody>
                
                
                    <tr>
                        <th scope="row">@Model.fact.id</th>
                        <td>@Model.fact.Clients</td>
                        <td>@Model.fact.Hourofsupport</td>
                        <td>@Model.fact.Projects</td>
                        <td>@Model.fact.workers</td>
                        <td>
                            <a class="btn btn-success" asp-page="UpdateBreakfast" asp-route-id="@Model.fact.id">Update</a>
                        </td>
                        <td>
                            <a class="btn btn-danger">Delete</a>
                        </td>
                    </tr>
                


            </tbody>
        </table>
    </div>
</div>